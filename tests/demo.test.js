describe('firstTes', () => { 
    test('Primera prueba en jest', ()=>{
        //1. Inicialización
        const message = 'message 1'
        //2. Estímulo
        const message2 = message.trim();
        //3. Observar comportamiento 
        //expect(message).toBe(message2);
        expect(message).toBe(message2)
        
    })


 })

